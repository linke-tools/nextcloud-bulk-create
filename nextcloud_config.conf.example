# Nextcloud server URL (without https://)
# Example: cloud.example.com
NC_URL=nextcloud.example.com

# Admin user credentials
# The user must have admin privileges to create users
NC_USER=admin
NC_PASS=password

# Default group for new users
# All created users will be added to this group
NC_GROUP=users

# Suffix to append to userids
# Example: @example.com will create users like "john.doe@example.com"
NC_USERID_SUFFIX=@example.com

# Rate limiting retry settings
# NC_RETRY_COUNT: Number of retries when hitting rate limits (first attempt + N retries)
# NC_RETRY_INTERVAL: Seconds to wait between retries (recommended: 180 or higher)
NC_RETRY_COUNT=3
NC_RETRY_INTERVAL=180
